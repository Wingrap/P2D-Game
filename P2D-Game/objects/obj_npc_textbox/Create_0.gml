text = "Do not do anything with this pls" // Text what NPC has to say
page = 0; // Page
charCount = 0; // Character count 
name = noone; // Name of NPC

xOffset = 3; // Offset on the X axis

boxWidth = sprite_get_width(spr_textbox_test); // Width of the text box
stringHeight = string_height(text); // Height of the string
